# UX Fixes — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix 3 UX issues — PWA session persistence, messages screen legibility, and landing page mobile nav.

**Architecture:** Surgical edits to 4 files (`session.ts`, `mensagens.tsx` ×2, `_index.tsx`). No new abstractions needed; new `PublicNav` component lives inline in `_index.tsx` for simplicity.

**Tech Stack:** React 19, React Router 7, Tailwind CSS v4, Lucide React, TypeScript 5.9, Bun

---

## Files Changed

| File | Action | Why |
|------|--------|-----|
| `Projeto/apps/web/app/lib/auth/session.ts` | Modify | `sessionStorage` → `localStorage` |
| `Projeto/apps/web/app/routes/jogador/mensagens.tsx` | Modify | Uppercase/tracking fixes + placeholder |
| `Projeto/apps/web/app/routes/time/mensagens.tsx` | Modify | Same fixes (duplicate file) |
| `Projeto/apps/web/app/routes/_index.tsx` | Modify | Add mobile bottom nav |

---

## Task 1: Fix PWA Session Persistence

**Files:**
- Modify: `Projeto/apps/web/app/lib/auth/session.ts`

- [ ] **Step 1: Open the file and read current content**

```bash
cat Projeto/apps/web/app/lib/auth/session.ts
```

Expected: 3 functions using `sessionStorage`.

- [ ] **Step 2: Replace all `sessionStorage` with `localStorage`**

The file has exactly 3 occurrences (lines 8, 18, 23). Replace each:

```typescript
import type { SessionUser } from "~shared/contracts";

const SESSION_STORAGE_KEY = "varzeapro_user";

export function getStoredUser(): SessionUser | null {
  if (typeof window === "undefined") return null;
  try {
    const raw = localStorage.getItem(SESSION_STORAGE_KEY);
    if (!raw) return null;
    return JSON.parse(raw) as SessionUser;
  } catch {
    return null;
  }
}

export function setStoredUser(user: SessionUser): void {
  if (typeof window === "undefined") return;
  localStorage.setItem(SESSION_STORAGE_KEY, JSON.stringify(user));
}

export function clearStoredUser(): void {
  if (typeof window === "undefined") return;
  localStorage.removeItem(SESSION_STORAGE_KEY);
}

/** Check if we have user data in storage (cookie-based auth, no token in JS). */
export function hasSession(): boolean {
  return !!getStoredUser();
}
```

- [ ] **Step 3: Verify no remaining `sessionStorage` references in auth**

```bash
grep -r "sessionStorage" Projeto/apps/web/app/lib/auth/
```

Expected: no output.

- [ ] **Step 4: Commit**

```bash
git add Projeto/apps/web/app/lib/auth/session.ts
git commit -m "fix(pwa): persist session in localStorage so PWA survives app close"
```

---

## Task 2: Messages UX — Jogador

**Files:**
- Modify: `Projeto/apps/web/app/routes/jogador/mensagens.tsx`

Changes needed (by line):
- **L147** — conversation list last message preview: remove `uppercase tracking-widest`
- **L250** — message body `<p>`: add `break-words`
- **L255** — timestamp `<p>` className: remove `uppercase tracking-widest`
- **L288** — Input placeholder: lowercase

- [ ] **Step 1: Fix conversation list last message preview (L147)**

Find:
```tsx
<span className="font-bold tracking-widest text-[11px] text-muted-foreground uppercase truncate block opacity-80">
  {c.lastMessage.content}
</span>
```

Replace with:
```tsx
<span className="font-bold text-[11px] text-muted-foreground truncate block opacity-80">
  {c.lastMessage.content}
</span>
```

- [ ] **Step 2: Fix message body — add break-words (L250)**

Find:
```tsx
<p className="text-lg font-medium leading-relaxed">
  {msg.content}
</p>
```

Replace with:
```tsx
<p className="text-lg font-medium leading-relaxed break-words">
  {msg.content}
</p>
```

- [ ] **Step 3: Fix timestamp — remove uppercase + tracking-widest (L255)**

Find:
```tsx
"mt-3 text-[10px] font-bold tracking-widest uppercase flex items-center justify-end border-t border-dashed border-foreground/20 pt-2",
```

Replace with:
```tsx
"mt-3 text-[10px] font-bold flex items-center justify-end border-t border-dashed border-foreground/20 pt-2",
```

- [ ] **Step 4: Fix input placeholder (L288)**

Find:
```tsx
placeholder="DIGITE SUA MENSAGEM..."
```

Replace with:
```tsx
placeholder="Digite sua mensagem..."
```

- [ ] **Step 5: Verify changes**

```bash
grep -n 'uppercase\|tracking-widest\|DIGITE' Projeto/apps/web/app/routes/jogador/mensagens.tsx
```

Expected output should only show uppercase/tracking-widest on UI chrome (headers, titles) — NOT on lines 147, 255. Line 288 should no longer have `DIGITE`.

- [ ] **Step 6: Commit**

```bash
git add Projeto/apps/web/app/routes/jogador/mensagens.tsx
git commit -m "fix(ux): mensagens jogador — remove uppercase from message content, fix overflow"
```

---

## Task 3: Messages UX — Time

**Files:**
- Modify: `Projeto/apps/web/app/routes/time/mensagens.tsx`

Same changes as Task 2. Lines are identical (files are near-copies).

- [ ] **Step 1: Fix conversation list last message preview (L147)**

Find:
```tsx
<span className="font-bold tracking-widest text-[11px] text-muted-foreground uppercase truncate block opacity-80">
  {c.lastMessage.content}
</span>
```

Replace with:
```tsx
<span className="font-bold text-[11px] text-muted-foreground truncate block opacity-80">
  {c.lastMessage.content}
</span>
```

- [ ] **Step 2: Fix message body — add break-words (L250)**

Find:
```tsx
<p className="text-lg font-medium leading-relaxed">
  {msg.content}
</p>
```

Replace with:
```tsx
<p className="text-lg font-medium leading-relaxed break-words">
  {msg.content}
</p>
```

- [ ] **Step 3: Fix timestamp — remove uppercase + tracking-widest (L255)**

Find:
```tsx
"mt-3 text-[10px] font-bold tracking-widest uppercase flex items-center justify-end border-t border-dashed border-foreground/20 pt-2",
```

Replace with:
```tsx
"mt-3 text-[10px] font-bold flex items-center justify-end border-t border-dashed border-foreground/20 pt-2",
```

- [ ] **Step 4: Fix input placeholder (L288)**

Find:
```tsx
placeholder="ENVIE UMA PROPOSTA OU MENSAGEM..."
```

Replace with:
```tsx
placeholder="Envie uma proposta ou mensagem..."
```

- [ ] **Step 5: Verify changes**

```bash
grep -n 'uppercase\|tracking-widest\|ENVIE\|DIGIT' Projeto/apps/web/app/routes/time/mensagens.tsx
```

Expected: uppercase/tracking-widest only on headers (lines 89, 92, 104, 107, 143, 178, 210, 215, 226). Not on lines 147, 255, 288.

- [ ] **Step 6: Commit**

```bash
git add Projeto/apps/web/app/routes/time/mensagens.tsx
git commit -m "fix(ux): mensagens time — remove uppercase from message content, fix overflow"
```

---

## Task 4: Landing Page Mobile Bottom Nav

**Files:**
- Modify: `Projeto/apps/web/app/routes/_index.tsx`

**Goal:** Add a bottom nav (`md:hidden`) with 4 items — Início, Planos, Entrar, Cadastrar — using the same brutalist style as AppShell's mobile nav.

- [ ] **Step 1: Add imports to `_index.tsx`**

Find the existing import block (top of file). Add `useLocation` to the React Router import and the needed Lucide icons:

Find:
```tsx
import { useEffect, useState } from "react";
import { Link, useNavigate } from "react-router";
```

Replace with:
```tsx
import { useEffect, useState } from "react";
import { Link, useNavigate, useLocation } from "react-router";
import { Home, Zap, LogIn, UserPlus } from "lucide-react";
import { cn } from "~/lib/utils";
```

- [ ] **Step 2: Add the PublicNav component before the `export default function Index()`**

Add this block immediately before `export default function Index()`:

```tsx
const PUBLIC_NAV = [
  { label: "Início", href: "/", icon: Home },
  { label: "Planos", href: "/planos", icon: Zap },
  { label: "Entrar", href: "/login", icon: LogIn },
  { label: "Cadastrar", href: "/cadastro", icon: UserPlus },
]

function PublicNav() {
  const location = useLocation()
  return (
    <nav className="fixed bottom-0 left-0 right-0 z-30 border-t-4 border-foreground bg-background md:hidden">
      <div className="flex h-16 items-stretch">
        {PUBLIC_NAV.map((item) => {
          const active = location.pathname === item.href
          return (
            <Link
              key={item.href}
              to={item.href}
              className={cn(
                "relative flex flex-1 flex-col items-center justify-center gap-1 text-[10px] font-bold tracking-widest uppercase transition-colors border-r-2 border-foreground/20 last:border-r-0",
                active
                  ? "bg-foreground text-background"
                  : "text-muted-foreground hover:bg-muted",
              )}
            >
              {active && (
                <div className="absolute top-0 left-0 w-full h-1 bg-primary" />
              )}
              <item.icon
                className={cn(
                  "size-6",
                  active ? "text-primary" : "text-foreground",
                )}
              />
              <span className="sr-only sm:not-sr-only sm:mt-1">
                {item.label}
              </span>
            </Link>
          )
        })}
      </div>
      <div className="h-[env(safe-area-inset-bottom)] bg-background" />
    </nav>
  )
}
```

- [ ] **Step 3: Add `<PublicNav />` and bottom padding to the landing page return**

The `Index` component returns a large `<div>` when user is NOT logged in. Find the opening of that div:

Find:
```tsx
  return (
    <div className="flex min-h-screen flex-col bg-background selection:bg-primary selec
```

This div wraps the entire landing page. Find the closing `</div>` at the very end of the component (before the final `}`) and add `<PublicNav />` right before it, and add `pb-16 md:pb-0` to the wrapper div so content isn't hidden behind the nav on mobile.

Find the wrapper div opening tag (it will look like):
```tsx
    <div className="flex min-h-screen flex-col bg-background selection:bg-primary selection:text-primary-foreground">
```

Replace with:
```tsx
    <div className="flex min-h-screen flex-col bg-background selection:bg-primary selection:text-primary-foreground pb-16 md:pb-0">
```

Then find the very last closing `</div>` in the non-logged-in return (right before the final `}`), and add `<PublicNav />` right before it:

```tsx
      <PublicNav />
    </div>
  )
}
```

- [ ] **Step 4: Verify no TypeScript errors**

```bash
cd Projeto/apps/web && npx tsc --noEmit 2>&1 | head -30
```

Expected: no errors. If errors appear about `cn` or icon imports, check that `~/lib/utils` exports `cn` (it does — confirmed in codebase).

- [ ] **Step 5: Verify the nav renders only on mobile**

Open the app in browser at `/`. Resize window below `md` breakpoint (< 768px) — bottom nav should appear. Above 768px — hidden.

- [ ] **Step 6: Commit**

```bash
git add Projeto/apps/web/app/routes/_index.tsx
git commit -m "feat(ux): add mobile bottom nav to landing page for PWA navigation"
```

---

## Task 5: Final Smoke Test

- [ ] **Step 1: Start dev server**

```bash
cd Projeto/apps/web && bun run dev
```

- [ ] **Step 2: Test PWA session**

1. Login with any account
2. Close the browser tab completely
3. Reopen the app at `http://localhost:5173`
4. Expected: still logged in, no redirect to `/login`

- [ ] **Step 3: Test messages screen**

1. Go to `/jogador/mensagens` (or `/time/mensagens`)
2. Send a message with mixed-case text like "Olá, tudo bem?"
3. Expected: text renders in normal case (not ALL CAPS)
4. Check the conversation list preview — also normal case
5. Check placeholder in input: "Digite sua mensagem..."

- [ ] **Step 4: Test landing nav**

1. Open `http://localhost:5173` logged out
2. Resize to mobile viewport (< 768px) in DevTools
3. Expected: bottom nav with Início/Planos/Entrar/Cadastrar appears
4. Above 768px: bottom nav hidden

- [ ] **Step 5: Final commit if any cleanup needed**

```bash
git add -A
git commit -m "chore: smoke test cleanup" # only if changes were needed
```
