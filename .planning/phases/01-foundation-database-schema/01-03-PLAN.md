---
phase: 01-foundation-database-schema
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - Projeto/apps/api/src/plugins/db.ts
  - Projeto/apps/api/src/app.ts
  - Projeto/apps/api/Dockerfile
  - Projeto/apps/api/.env.example
  - Projeto/docker-compose.yml
  - Projeto/.env.example
  - Projeto/.gitignore
  - .github/workflows/test.yml
  - .github/workflows/build.yml
  - .github/workflows/deploy.yml
  - Projeto/apps/api/tests/migrations.test.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "Docker Compose brings up postgres, api, and web containers"
    - "api container waits for postgres to be healthy before starting"
    - "Drizzle migrations run and create all 8 tables in the database"
    - "GET /health returns 200 from the running containerized API"
    - "GitHub Actions CI/CD pipeline stubs exist for test, build, deploy"
    - "fastify.db decorator is available in route handlers"
  artifacts:
    - path: "Projeto/docker-compose.yml"
      provides: "Multi-service Docker Compose stack"
      contains: "postgres"
    - path: "Projeto/apps/api/Dockerfile"
      provides: "Multi-stage API container build"
      contains: "node:20-alpine"
    - path: "Projeto/apps/api/src/plugins/db.ts"
      provides: "Drizzle DB decorator plugin"
      contains: "fastify.decorate"
    - path: ".github/workflows/test.yml"
      provides: "CI test pipeline"
      contains: "vitest"
    - path: ".github/workflows/build.yml"
      provides: "CI build pipeline"
      contains: "docker"
    - path: ".github/workflows/deploy.yml"
      provides: "CD deploy pipeline"
      contains: "deploy"
  key_links:
    - from: "Projeto/apps/api/src/plugins/db.ts"
      to: "Projeto/apps/api/src/db/schema/index.ts"
      via: "import * as schema from '../db/schema'"
      pattern: "import.*schema"
    - from: "Projeto/apps/api/src/app.ts"
      to: "Projeto/apps/api/src/plugins/db.ts"
      via: "fastify.register(import('./plugins/db.js'))"
      pattern: "register.*plugins/db"
    - from: "Projeto/docker-compose.yml"
      to: "Projeto/apps/api/Dockerfile"
      via: "build: ./apps/api"
      pattern: "build.*apps/api"
    - from: "Projeto/docker-compose.yml"
      to: "postgres healthcheck"
      via: "condition: service_healthy"
      pattern: "service_healthy"
---

<objective>
Wire the Fastify server to the Drizzle schema via a DB plugin, create the Docker Compose stack (postgres + api + web), write the API Dockerfile, generate the initial Drizzle migration, and create GitHub Actions CI/CD pipeline stubs.

Purpose: This completes Phase 1 by connecting all pieces — the server (Plan 01) talks to the database (Plan 02) inside a containerized environment. CI/CD stubs are scaffolded now for INFRA-02, to be fully exercised in Phase 7.
Output: Working Docker Compose stack, DB plugin, initial migration, CI/CD workflow files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-database-schema/01-RESEARCH.md
@.planning/phases/01-foundation-database-schema/01-01-SUMMARY.md
@.planning/phases/01-foundation-database-schema/01-02-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 outputs -->
From Projeto/apps/api/src/app.ts:
```typescript
export async function buildApp(): Promise<FastifyInstance>
// Registers: env plugin, cors plugin, sensible, health routes
// DB plugin registration slot is between sensible and routes
```

From Projeto/apps/api/src/config/env.ts:
```typescript
export type Env = {
  NODE_ENV: "development" | "production" | "test"
  PORT: number
  DATABASE_URL: string
  JWT_SECRET: string
  CORS_ORIGIN: string
}
// Available as fastify.config after env plugin registers
```

<!-- From Plan 02 outputs -->
From Projeto/apps/api/src/db/schema/index.ts:
```typescript
export { users, roleEnum, planIdEnum } from "./users"
export { players } from "./players"
export { teams, teamLevelEnum } from "./teams"
export { conversations } from "./conversations"
export { messages } from "./messages"
export { subscriptions, subscriptionStatusEnum } from "./subscriptions"
export { auditLogs } from "./audit-logs"
export { moderationReports, reportedEntityTypeEnum, reportReasonEnum, reportStatusEnum } from "./moderation-reports"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create DB plugin, wire into app.ts, write Dockerfile and Docker Compose</name>
  <files>
    Projeto/apps/api/src/plugins/db.ts
    Projeto/apps/api/src/app.ts
    Projeto/apps/api/Dockerfile
    Projeto/apps/api/.env.example
    Projeto/docker-compose.yml
    Projeto/.env.example
    Projeto/.gitignore
  </files>
  <read_first>
    Projeto/apps/api/src/app.ts
    Projeto/apps/api/src/config/env.ts
    Projeto/apps/api/src/db/schema/index.ts
    Projeto/apps/api/package.json
    Projeto/apps/web/Dockerfile
    .planning/phases/01-foundation-database-schema/01-RESEARCH.md
  </read_first>
  <action>
    1. `src/plugins/db.ts`: Create the Drizzle decorator plugin:
       ```typescript
       import fp from "fastify-plugin"
       import { drizzle, type PostgresJsDatabase } from "drizzle-orm/postgres-js"
       import postgres from "postgres"
       import * as schema from "../db/schema/index.js"

       export default fp(async (fastify) => {
         const client = postgres(fastify.config.DATABASE_URL)
         const db = drizzle(client, { schema })
         fastify.decorate("db", db)
         fastify.addHook("onClose", async () => {
           await client.end()
         })
       })

       declare module "fastify" {
         interface FastifyInstance {
           db: PostgresJsDatabase<typeof schema>
         }
       }
       ```
       CRITICAL: Must use `fp()` wrapper so `fastify.db` is visible outside plugin scope.

    2. Update `src/app.ts`: Add the DB plugin registration AFTER env and BEFORE routes:
       ```typescript
       // After: await fastify.register(import("./config/env.js"))
       await fastify.register(import("./plugins/db.js"))
       // Before: await fastify.register(import("./routes/health.js"), { prefix: "/health" })
       ```
       The registration order must be: env -> db -> cors -> sensible -> error handler -> routes.

    3. `Projeto/apps/api/.env.example`:
       ```
       NODE_ENV=development
       PORT=3000
       DATABASE_URL=postgresql://varzeapro:varzeapro_dev@localhost:5432/varzeapro
       JWT_SECRET=change-me-to-a-real-secret-at-least-32-chars-long
       CORS_ORIGIN=http://localhost:5173
       ```

    4. `Projeto/apps/api/Dockerfile` (multi-stage, NO ARG for secrets):
       ```dockerfile
       FROM node:20-alpine AS deps
       WORKDIR /app
       COPY package*.json ./
       RUN npm ci --omit=dev

       FROM node:20-alpine AS builder
       WORKDIR /app
       COPY package*.json ./
       RUN npm ci
       COPY . .
       RUN npm run build

       FROM node:20-alpine AS runner
       WORKDIR /app
       ENV NODE_ENV=production
       COPY --from=deps /app/node_modules ./node_modules
       COPY --from=builder /app/dist ./dist
       COPY --from=builder /app/package.json ./
       EXPOSE 3000
       CMD ["node", "dist/server.js"]
       ```
       CRITICAL: Do NOT use `ARG` for secrets. Secrets are injected at runtime via `env_file`.

    5. `Projeto/docker-compose.yml`:
       ```yaml
       services:
         postgres:
           image: postgres:16-alpine
           environment:
             POSTGRES_DB: varzeapro
             POSTGRES_USER: varzeapro
             POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-varzeapro_dev}
           healthcheck:
             test: ["CMD-SHELL", "pg_isready -U varzeapro"]
             interval: 5s
             timeout: 5s
             retries: 5
           ports:
             - "5432:5432"
           volumes:
             - postgres_data:/var/lib/postgresql/data

         api:
           build: ./apps/api
           depends_on:
             postgres:
               condition: service_healthy
           env_file: .env
           environment:
             DATABASE_URL: postgresql://varzeapro:${POSTGRES_PASSWORD:-varzeapro_dev}@postgres:5432/varzeapro
           ports:
             - "3000:3000"

         web:
           build: ./apps/web
           depends_on:
             - api
           environment:
             VITE_API_URL: http://api:3000
           ports:
             - "5173:3000"

       volumes:
         postgres_data:
       ```
       CRITICAL: postgres has `healthcheck` with `pg_isready`. api uses `condition: service_healthy`.

    6. `Projeto/.env.example`:
       ```
       # Docker Compose environment
       POSTGRES_PASSWORD=varzeapro_dev
       NODE_ENV=development
       PORT=3000
       DATABASE_URL=postgresql://varzeapro:varzeapro_dev@postgres:5432/varzeapro
       JWT_SECRET=change-me-to-a-real-secret-at-least-32-chars-long
       CORS_ORIGIN=http://localhost:5173
       ```

    7. Ensure `Projeto/.gitignore` contains (create or append):
       ```
       .env
       node_modules/
       dist/
       ```

    8. Generate initial Drizzle migration:
       ```bash
       cd Projeto/apps/api
       DATABASE_URL=postgresql://unused:unused@localhost:5432/unused npx drizzle-kit generate
       ```
       This generates SQL migration files in `src/db/migrations/`. The DATABASE_URL value doesn't matter for generation — it only reads the schema files.
  </action>
  <verify>
    <automated>cd Projeto/apps/api && grep -q "fastify.decorate" src/plugins/db.ts && grep -q "plugins/db" src/app.ts && test -f Dockerfile && test -f ../../docker-compose.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `Projeto/apps/api/src/plugins/db.ts` contains `import fp from "fastify-plugin"`
    - `Projeto/apps/api/src/plugins/db.ts` contains `fastify.decorate("db", db)`
    - `Projeto/apps/api/src/plugins/db.ts` contains `fastify.addHook("onClose"`
    - `Projeto/apps/api/src/plugins/db.ts` contains `import * as schema from "../db/schema`
    - `Projeto/apps/api/src/app.ts` contains `import("./plugins/db.js")`
    - `Projeto/apps/api/Dockerfile` contains `FROM node:20-alpine` and does NOT contain `ARG JWT_SECRET` or `ARG DATABASE_URL`
    - `Projeto/apps/api/Dockerfile` contains `CMD ["node", "dist/server.js"]`
    - `Projeto/docker-compose.yml` contains `postgres:` service with `healthcheck`
    - `Projeto/docker-compose.yml` contains `condition: service_healthy`
    - `Projeto/docker-compose.yml` contains `api:` and `web:` services
    - `Projeto/.env.example` contains `POSTGRES_PASSWORD` and `JWT_SECRET`
    - `Projeto/apps/api/.env.example` contains `DATABASE_URL`
    - `Projeto/apps/api/src/db/migrations/` directory exists with at least one `.sql` file
  </acceptance_criteria>
  <done>
    DB plugin decorates Fastify with typed `fastify.db`. app.ts registers DB plugin in correct order. Dockerfile builds multi-stage without secrets in ARG. Docker Compose defines 3 services with postgres healthcheck. Initial Drizzle migration is generated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub Actions CI/CD pipeline stubs</name>
  <files>
    .github/workflows/test.yml
    .github/workflows/build.yml
    .github/workflows/deploy.yml
  </files>
  <read_first>
    .planning/phases/01-foundation-database-schema/01-RESEARCH.md
  </read_first>
  <action>
    1. `.github/workflows/test.yml`:
       ```yaml
       name: Test
       on:
         push:
           branches: [main, dev]
         pull_request:
           branches: [main]

       jobs:
         test:
           runs-on: ubuntu-latest
           services:
             postgres:
               image: postgres:16-alpine
               env:
                 POSTGRES_DB: varzeapro_test
                 POSTGRES_USER: varzeapro
                 POSTGRES_PASSWORD: test_password
               ports:
                 - 5432:5432
               options: >-
                 --health-cmd "pg_isready -U varzeapro"
                 --health-interval 10s
                 --health-timeout 5s
                 --health-retries 5
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with:
                 node-version: 20
                 cache: npm
                 cache-dependency-path: Projeto/apps/api/package-lock.json
             - run: npm ci
               working-directory: Projeto/apps/api
             - run: npx drizzle-kit migrate
               working-directory: Projeto/apps/api
               env:
                 DATABASE_URL: postgresql://varzeapro:test_password@localhost:5432/varzeapro_test
             - run: npm test
               working-directory: Projeto/apps/api
               env:
                 NODE_ENV: test
                 DATABASE_URL: postgresql://varzeapro:test_password@localhost:5432/varzeapro_test
                 JWT_SECRET: test-secret-that-is-at-least-32-characters-long
                 CORS_ORIGIN: http://localhost:5173
       ```

    2. `.github/workflows/build.yml`:
       ```yaml
       name: Build
       on:
         push:
           branches: [main]
         pull_request:
           branches: [main]

       jobs:
         build-api:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - uses: actions/setup-node@v4
               with:
                 node-version: 20
                 cache: npm
                 cache-dependency-path: Projeto/apps/api/package-lock.json
             - run: npm ci
               working-directory: Projeto/apps/api
             - run: npm run build
               working-directory: Projeto/apps/api

         build-docker:
           runs-on: ubuntu-latest
           steps:
             - uses: actions/checkout@v4
             - run: docker compose -f Projeto/docker-compose.yml build
       ```

    3. `.github/workflows/deploy.yml`:
       ```yaml
       name: Deploy
       on:
         push:
           branches: [main]

       # Stub — full deployment logic will be configured in Phase 7
       jobs:
         deploy:
           runs-on: ubuntu-latest
           if: github.ref == 'refs/heads/main'
           steps:
             - uses: actions/checkout@v4
             - name: Deploy placeholder
               run: echo "Deploy step — configure VPS deployment in Phase 7"
       ```
  </action>
  <verify>
    <automated>test -f .github/workflows/test.yml && test -f .github/workflows/build.yml && test -f .github/workflows/deploy.yml && grep -q "vitest\|npm test" .github/workflows/test.yml && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test.yml` exists and contains `postgres:16-alpine` service
    - `.github/workflows/test.yml` contains `npm test` or `vitest` run step
    - `.github/workflows/test.yml` contains `drizzle-kit migrate` step
    - `.github/workflows/build.yml` exists and contains `npm run build`
    - `.github/workflows/build.yml` contains `docker compose` build step
    - `.github/workflows/deploy.yml` exists and contains placeholder deploy step
    - All 3 files use `actions/checkout@v4` and `actions/setup-node@v4`
  </acceptance_criteria>
  <done>
    Three GitHub Actions workflow files exist: test.yml runs Vitest with a Postgres service container, build.yml compiles TypeScript and builds Docker images, deploy.yml is a stub for Phase 7 VPS deployment.
  </done>
</task>

</tasks>

<verification>
- `grep "fastify.decorate" Projeto/apps/api/src/plugins/db.ts` — DB decorator present
- `grep "plugins/db" Projeto/apps/api/src/app.ts` — DB plugin registered
- `test -f Projeto/docker-compose.yml` — Docker Compose exists
- `grep "service_healthy" Projeto/docker-compose.yml` — healthcheck wiring present
- `ls .github/workflows/` — test.yml, build.yml, deploy.yml exist
- `ls Projeto/apps/api/src/db/migrations/*.sql` — migration files generated
</verification>

<success_criteria>
- DB plugin creates `fastify.db` typed decorator with Drizzle + full schema
- app.ts registers DB plugin in correct order (after env, before routes)
- Dockerfile builds multi-stage without secrets in ARG layers
- Docker Compose defines postgres (with healthcheck), api, and web services
- Initial Drizzle migration SQL files exist
- GitHub Actions test/build/deploy workflow files exist
- `.env.example` files document all required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-database-schema/01-03-SUMMARY.md`
</output>
