name: Deploy
on:
  push:
    branches: [main]

# Stub — full deployment logic will be configured in Phase 7
jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy placeholder
        run: echo "Deploy step — configure VPS deployment in Phase 7"
