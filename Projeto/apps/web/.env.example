# API base URL (optional). When unset, uses /api (same origin). Set to real API URL when backend is available.
# VITE_API_URL=http://localhost:3000/api

# Enable mock API (MSW). Use "true" to force mock in production build; in dev, mock is on by default unless set to "false".
# VITE_USE_MOCK=true
